# Revora panel environment variables.
# Copy to .env for local development. SvelteKit exposes PUBLIC_* vars
# to the browser (import.meta.env.PUBLIC_*).

# Revora API base URL (include /v1 prefix).
# Production Railway service per AGENT_USAGE.md:
PUBLIC_REVORA_API_URL=https://backend-production-5c03.up.railway.app/v1

# Operating mode: 'standalone' (Mode A) or 'hoops' (Mode B1 — embed).
# See spec §1.1. Phase 1 uses 'standalone'.
PUBLIC_REVORA_MODE=standalone
